# InputStream
  Stream Size:
      Null.                              [single][error]
      Empty.                      [single][property emptystream]
      Not Empty.

  Stream behavior during read:
      Normal read.
      IOException.           [single][error]

# Encoding
  Encoding value:
      Null.                              [single][error]
      Valid encoding.
      Unsupported encoding.              [single][error]

# Buffer Size
Buffer size value:
    Zero.                              [single][error]
    Negative.                          [single][error]
    One.                               [single]
    Smaller than content.              [if !emptystream][property smallerbuffer]
    Equal to content size.             [if !emptystream][property equalbuffer]
    Larger than content size.          [single][if !emptystream][property largerbuffer]

Content length relative to buffer:
    Fits in initial buffer.            [single][if largerbuffer]
    Requires one resize.               [if smallerbuffer || equalbuffer]
    Requires multiple resizes.         [single][if smallerbuffer]
